project:
  type: website
  # 如果你把 Pages 设成输出到 docs/，就加上下一行
  # output-dir: docs

format:
  html:
    theme: cosmo
    highlight-style: github     # 代码配色：github / monokai / dracula / atom-one ...
    code-copy: true             # 代码块右上角复制按钮
    code-line-numbers: false    # 需要行号就改成 true
    css: assets/quarto-overrides.css  # 自定义代码框样式（下一步）

